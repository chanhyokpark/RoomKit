/**
 * Re-export of @roomkit/hintphone-core. In the published dist this file is
 * replaced by a self-contained tsup bundle (see tsup.config.ts) — everything
 * in this package must import the core through './core.js', never from
 * '@roomkit/hintphone-core' directly.
 */
export * from '@roomkit/hintphone-core';
